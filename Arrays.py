rows = 4
columns = 6
print "a %r x %r array (rows x columns)" % (rows, columns)
#creates an array with dimensions rows by columns
myArray = [[0 for j in range(columns)] for i in range(rows)]
print myArray
